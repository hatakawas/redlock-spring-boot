language: java
sudo: enabled
jdk:
  - oraclejdk8
install: true
script:
  - ./mvnw clean install
  - bash <(curl -s https://codecov.io/bash)
cache:
  directories:
    - $HOME/.m2
services:
  - redis-server